# 2026-09-13

## pi2 文档中译（第二批：agent/docs 之外的全部文档）

- 产出：`pi2-studydoc/` 新增 80 个 .md（21,881 行），目录结构与源 `pi2-source-code/pi` 一致
- 范围（用户确认）：文档主体，**跳过 CHANGELOG.md（9 个）与 test/fixtures（14 个 SKILL.md fixture）**
- 覆盖：
  - `packages/coding-agent/docs/` 30 篇（extensions 3033 行、rpc 1618、sdk 1226、tui 961、custom-provider 777…）
  - 11 个包 README（ai 1715、tui 903、agent 517、telemetry 464、evals、protocol、server、client、session-backends、benchmark）
  - `packages/chord/PLANNING.md`(860) + README + `src/delta/README.md`
  - 根 `README.md`/`AGENTS.md`/`CONTRIBUTING.md`/`SECURITY.md`/`tui-plan.md`(1001)
  - `.pi/prompts/*`（6）+ `.pi/skills/*`（3）
  - `examples/**` 16 篇 + `src/experimental/{mini,services}/README.md`
- 方法：13 个并行子代理分组翻译；统一规则同第一批，新增「YAML frontmatter：标识符字段保留原文、description 译中文」

### 最终校验（两批合计 119 文件）

| 项 | 结果 |
|---|---|
| 文件齐全 | 119/119，0 缺失 |
| 行数 | 源 46,536 → 译 46,579 |
| 标题/表格行数逐文件比对 | 0 处不一致 |
| 代码块逐字节 diff | 仅 5 文件有偏差，全部为 ASCII 图标签译中文（rate-limiting / extensions / subagent README / mini README / tui-plan） |

### 踩坑与修正

1. **frontmatter 在代码块内被误译**：`prompt-templates.md`、`skills.md` 的 YAML 示例块中 `description` 被译成中文，与「代码块原样保留」冲突。已用脚本把这两个文件的差异块整体还原为源文，确立不变量：**除 ASCII 图标签外，代码块逐字节一致**。
2. **CJK 宽度陷阱**：用 `len()` 或 `awk length()` 校验 ASCII 图对齐会误报（中文字符宽度 2）。必须用 `unicodedata.east_asian_width` 计算显示宽度。
3. **分组遗漏**：`environment-variables.md`、`terminal-setup.md` 未分配，已补译。
4. **并行进程干扰**：根目录研究报告被并行进程追加 §7 Chord 章节（744 行），已保留并在回复中告知用户。

### 可复用脚本（/tmp，易失，需要时可重写）

- 结构对齐 + 代码块 diff + 未翻译英文散文扫描：`verify3.py` / `final.py`
- 差异代码块定位：`blk.py`
- 差异代码块还原为源文：`fix.py`
