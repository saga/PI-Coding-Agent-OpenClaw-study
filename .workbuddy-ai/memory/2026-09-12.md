# 2026-09-12

## pi2 (pi.dev) 架构研究

- 产出：`pi2-pi-dev-代码架构与AgentHarness研究.md`（仓库根目录）
- 分析对象：`pi2-source-code/pi`（earendil-works/pi，v0.85.1，HEAD 71dca871b）
- 关键结论：
  - 11 个 package；构建拓扑序 chord→tui→telemetry→ai→agent→session-backends→protocol→client→server→coding-agent
  - `pi-agent-core` 新增 `AgentHarness`：三存储不变式（entries 不可变树 / values+lists 可变 / usage ledger 只追加）
  - 13 个 flat operation state leaf，每次转移写完整总态作为唯一重启点
  - 四原语 accept / drive / requestAbort / inspectExecution；accept 与 drive 分离
  - 意图→效果→结算两段提交；工具 `replay: "safe" | "never"`；并行工具按完成顺序 stage、按源顺序 materialize
  - **重要事实**：稳定 `pi` CLI 仍用经典 `Agent` + JSONL SessionManager；`AgentHarness` 只出现在 `src/experimental/`（session-worker / mini / services）
  - 分布式骨架：chord（facets/services/replicated state）+ protocol v8（CBOR 分帧）+ server/client + experimental/mini
  - 官方自陈缺口：J1（JSONL 无物理回收）、C1（RemoteSession 契约矛盾）、S12/R12（watchSession stub）、S3（search 无实现）、WP08
- 对比基线：pi1 `pi-mono` v0.67.68（agent 包只有 agent-loop/agent，无 harness 目录）

## pi2 agent/docs 中文翻译

- 产出：`pi2-studydoc/`（39 个 .md，24,722 行），保持与源相同的目录结构
- 源：`pi2-source-code/pi/packages/agent/docs`（39 md / 24,655 行）
- 未翻译（非文档）：20 个 `.ts` / `.js` / `.json` / `.diff` 文件
- 方法：13 个并行子代理分组翻译 + 统一术语规则（类型名/标识符/路径保留英文，散文全译，代码块原样保留）
- 校验脚本（可复用）：结构对齐检查（标题数/``` 围栏数/表格行数逐文件比对）+ 代码块逐字节 diff + 未翻译英文散文扫描
  - 结果：39/39 文件结构完全对齐；代码块全部一致（仅 1 处 ASCII 图标签译为中文）；无漏译段落
- 注意：根目录的研究报告 `pi2-pi-dev-代码架构与AgentHarness研究.md` 被并行进程追加了 §7「Chord」章节（现 744 行，原 630 行）并重排后续章节号，已保留未回滚

## pi2 研究报告重构（execution / composition 双主线）

- 产出：重写 `pi2-pi-dev-代码架构与AgentHarness研究.md` → 1434 行（原 785 行）
- 用户编辑方针：**不砍 Chord**；把整篇重定义为「为了理解 Chord，先把它所在的问题空间铺平」
- 核心叙事锚点：**`AgentHarness` 解决 execution，`Chord` 解决 composition**（因果而非并列）
- 结构（13 节 + 阅读指引）：
  - §2.2 新增核心运行时视角图（Application / Chord[composition+boundary] / Presentation-Services-Worker / AgentHarness[execution+recovery] / Session）
  - §4 重命名为「问题一 · Execution」；§6 重命名为「问题二 · Composition」（新增 6.1 场景图 TUI/Web/Mobile/RPC/CI + 6.2 组合问题八问清单）
  - §7 Chord 全章重写为 7.1–7.11：为什么需要 / 一句话 / Facet / Service / Replicated State / Remote Boundary / FacetHost / Plugin Reload / 完整例子(TUI→Service→Worker→Harness) / 为什么必须分开 / 与 DSH 对应速览
  - §8 新增「Chord 与 DI」：8.3 为何拒绝 `ctx.get(name)` 代理模型；8.4 **能力边界**（Chord 真正交付物是 capability boundary 而非 DI，靠 `setup(env)` 参数表在类型层卡死）
  - §9 DSH/Cordis 对照重写：先分清 dsh（产品）与 Cordis（元框架，源自 Koishi）；分「概念相似 / 实现不同 / 架构位置不同」三层；核心结论 = 不是同一问题的两种解法，可组合（Cordis 管进程内、Chord 管进程间）
- 一手证据（工作区内）：`packages/agent/docs/mobile-handoff/02-plugins/01-facets/facets.md` §13.1–13.2 是 pi 对 Cordis/DSH 的直接批评（拒绝代理模型、四阶段退役）；`pi2-pi-dev-chatgpt分析的架构.md`（4956 行，第三方分析）
- 二手证据（web，2026-09-15 检索）：dsh 2026-08-13 发布（MIT，Developer Preview，CLI `dsh`）；Cordis 源自 Koishi（作者 Shigma），五核心概念 = 插件/上下文/注入/事件/可逆副作用
- 源码校验结论：`chord/src/types.ts:60` 只有 `ServiceMode = "singleton" | "keyed"`；`peer` 仅存在于 `facets.md` 规格（**规格超前实现**，已在正文标注）；`api.ts:80` 对 `$chord.` 前缀抛 TypeError；`wire.ts:39` `SERVICE_CONTROL_ID = "$chord.service"`
