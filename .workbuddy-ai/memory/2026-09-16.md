# 2026-09-16

## pi2 研究报告第二遍编辑（教学层补齐）

用户对 1434 行版给出 10 点评审，明确「不需要大改结构，只做第二遍编辑：每个 Chord 概念先用现实问题讲清楚，再放 API / 源码细节」。执行结果：**1434 → 1538 行**。

### 已改（用户点名 3 项 + 强信号 4 项）

1. **§7.1 前置具体 composition 场景**（用户点名 #1）：开头加 worker 进程 + 三个需求（TUI 显示进度 / RPC 远程发起 run / 插件注册 service）→ 直接暴露 Session/Harness/tool registry 的三个坏结果 → 逼出问题陈述"一个已可靠运行的 Agent 如何被拆开、连接、扩展，同时不暴露内部能力"。
2. **§7.3 补 `Plugin → Facet → Service` 三层关系**（用户点名 #3）：加三层对照块 + 金句「**Facet 是"东西住在哪里"，Service 是"它对外能做什么"**」。
3. **§7.4 补"为什么不直接传对象"对比**（用户点名 #2）：TUI→models 对象 vs TUI→Models Service→本地/远端实现；并前置 2 句 DI 定位（用户 #6）："Chord 是 DI 的远亲，关心能力在哪/谁提供/何时存在/谁能拿到"。
4. **§7.2 把"能力边界"提为 Chord 的第二核心定义**（用户 #4，称这是全文最有特色的观察）：新增「Chord 有两个核心问题」块（①怎么装起来=composition ②装起来后能拿到什么=capability boundary），并明确"第 2 问才是本报告认为最有价值的观察"；§0 结论速览第 6 条、§8.4 开头同步对齐。
5. **软化 Cordis/Chord 切割**（用户 #7）：§7.11 与 §9.4 的"程序内 vs 跨进程"改为"重心差异"，并加显式警告「**重心不同 ≠ 层级不同**」「读成两个互不相干的抽象层是本节最想避免的误读」；§9.4 主图标签改为"进程内的组装与插件树 / 跨环境的能力边界与状态"。
6. **主图贯穿全文**（用户 #9）：§7.6/§7.7/§7.9/§7.10 各加一句"回到 §2.2 主图"的回指；§7.9 明确定位为**本章总结图**。
7. **§3 降级为主线外**（用户 #10）：标题改为「`pi-ai`：Agent Runtime 依赖的模型层（主线外，可跳过）」，内容压成"AgentHarness 真正依赖的四件事"表（Models/ModelRuntime、AssistantMessageFrame、deferred handle、只收 function calling），删掉 provider 工厂/codex/overflow 等细节。

### 两层阅读约定（用户 #5，轻量版）

§7 章首加「阅读约定」：每个概念先用现实问题讲清、再用 `#### 实现细节` 展开；第一次读可跳过所有实现细节。已在 §7.3 / §7.4 / §7.5 插入 3 个 `#### 实现细节` 小标题（§7.5 的为「实现细节：Delta tracking」）。

### 未做

- 用户 #5 的"每章严格分两层"只做了 §7.3/7.4/7.5 三处（避免过度改结构）；§7.6–7.8 未加小标题。
- §3 未真正降级为子节（那需要 §4–§13 全部重编号，与"不需要大改结构"冲突），改为标题标注 + 内容压缩。
- §7.11 与 §9 的 DSH 对照内容未再改（用户 #7 只要求软化措辞）。

## pi2 研究报告第三次重构（中心句升级为 Model Input 编译器）

用户给出 14 点指令（"严格按照意见改"），把全文中心从"execution / composition 分工"升级为**"Agent Harness = Agent State → Model Input 的运行时编译器"**。执行结果：**1538 → 2038 行**。

### 新中心句（已写入 阅读指引 + §0 第 0 条）

> Agent Harness 的核心任务，是把"当前 Agent State"编译成下一次模型调用所需的完整上下文。**Execution** 负责保证状态不因崩溃/重试/副作用失真；**Composition** 负责决定哪些能力和上下文有资格进入这次编译。
> 更准确的表述（用户明确否掉"Harness 就是 Prompt 拼装器"这个过窄说法）：**Agent Harness 本质是"Agent State → Model Input"的运行时编译器，Prompt Assembly 是最核心的投影过程。**

### 结构变更

- **新增 `## 再换一个视角：Harness 最终到底在干什么？`**（紧跟阅读指引）：Model Input 的 8 项成分表 + 6 个必答问题（信息从哪来 / 谁决定 / 什么顺序 / 何时持久化 / 插件如何修改 / 崩溃后如何保证正确）。
- **新增 `### 全文主线`**：14 步流程图（Agent 为什么需要 Harness → 不是一次 LLM call → 每轮重构 Model Input → Context Assembly → 必须可靠 → Pi/AgentHarness → 谁可参与 assembly → Pi/Chord → 对照 DeepSeek/Cordis）。
- **§4 整章重构**：`问题一 · Execution：AgentHarness 核心` → **`AgentHarness：从持久状态到下一次 Model Input`**，由 13 个小节重排为 4.1–4.8（从 Session 到 Model Input / Session 是事实来源不是 Prompt / Context Projection / Compaction / Tool-Assistant Durability / accept-drive / Effect-Replay / 结论）。**机制内容全部保留**，但每节新增"如何影响下一次模型输入"的框架；新增 §4.1 投影链总图、§4.8 结论表（机制 → 它保证的事）。
- **新增 `### 6.8 收束：Agent Harness 的本质，是构造下一次 Model Input`**（放在 §6 末尾、紧邻 §7，因为用户要求"接着直接进入"Chord；用户原文写的是 6.5，但 6.5 位置在实验性实现之前，与"接着直接进入"矛盾，故改为 6.8）。
- **§7 改名**：`Chord：pi.dev 给这个问题的答案` → **`Chord：谁可以参与 Agent Runtime 的组合`**；7.4 → `Service：组件如何向 Context Assembly 提供能力`；7.5 → `Replicated State：状态如何进入其他运行环境`。
- **§8.4 增强**：新增"真正的风险不是它拿到一个内部对象，而是**它可以绕过受控的 Context Assembly 路径，直接改变模型看到的世界**"论证 + Plugin→Service/State/Hook→受控 Assembly 图。
- **§9 整章重写**：`与 DeepSeek Harness 的对照` → **`两种 Harness：谁控制 Model Input 的构造`**，9.1–9.8：两个系统各是什么 / **主对照表（10 行，按"谁构造模型输入"逐条问）** / 两张 Prompt Assembly 图 / 谁是内核（Pi 固定内核+外部组合 vs dsh agent-loop 也是 plugin） / "Everything is a Plugin" 的真正含义 / DSH system-prompt 机制 / API 层对照（降级为附）/ 可借的三件事。
- **§11 新增 3 条设计模式**（15 把"输入"当一等产物 / 16 保护投影链唯一性 / 17 prompt 属于派生历史不属于请求参数）。

### 新获取的一手证据（WebFetch，DSH 官方文档）

之前只有第三方报道，本次直接读取了 DeepSeek Harness 官方文档，§9 证据等级已升级：

- `docs/subsystems/system-prompt.md`：`PromptSection{name,order,text,interpolate?,complete?}`（text 可为每次 assembly 求值的 provider 函数；同名重复抛错；**多于一个 effective `complete` → assembly 失败**）；**`PromptContext` 是 `PromptSection` 的 "cache-safe" 对偶**（"Dynamic model context materialized as a durable user-role snapshot"，只在变化或被 compaction 移除时、在保留历史之后记录）；`ctx.systemPrompt` 提供 `section()/context()/tools(provider)/variable()/getSectionOrder()/getContextOrder()/suppressRuntimeContext()/assemble()`，**每个注册返回 Cordis effect disposer**；`system-prompt/assemble` 是 **waterfall**、`system-prompt/change` 是 emit；**prompt 由 agent-loop `renderPrompt` 渲染后提交为 `system/message` surface node**（首步 node 0，之后原地替换；`systemPromptUpdate:'in-history'` 时追加在缓存历史之后）→ 官方原话"**the prompt reaches the model as a message of derived history rather than as a request field**"。
- `docs/cordis-tutorial/index.md`：原文"**every capability — tools, LLM adapters, file access, the agent loop itself — is a plugin mounted into a shared context**"。
- `.agents/notes/implemented/architecture/2026-06-11-microkernel-event-taxonomy.md`：产品原则原文列出 **hooks / /goal / /loop / dynamic workflows / compaction / sandboxing / permissions / UI / persistence / MCP / skills 都必须可作为 plugin 编写而不改 core**；四类派发 waterfall（`agent/pre-step`、`agent/request`、`agent/request-error`、`tools/pre-execute`、`tools/execute`、`tools/post-execute`、`llm/stream`、**`system-prompt/assemble`**）/ serial（`agent/turn-stopping`）/ parallel（`session/flush`）/ emit；**`@deepseek-ai/dsh-agent-loop` 是唯一具体 loop plugin 且本身可替换，"nothing outside it may depend on it"**。

### 关键新洞察（值得记住）

- **最精确的差异**：pi 的 context projection 是**固定 5 步算法**（扩展点窄且显式：entryProjectors / transform_context / hooks）；dsh 的 **`system-prompt/assemble` 本身就是一个 waterfall 事件**，插件可包裹整个 assembly。两者是**两种可靠性策略**，不是先进/落后。
- **两边的独立共识**：① prompt 应作为"从事实投影出来的派生消息"而非请求字段（pi §4.2 / dsh system/message surface node）；② **中间插入会摧毁 provider KV cache** —— pi 用 append-only context invariant，dsh 用 cache-safe PromptContext 快照。同一个约束的两种工程表达。

### 遗留

- 事件/快照/reducer 一节保留在 §4.5 内（用户给的 8 节大纲没有为它留位置），略偏离主题但内容完整。
- §6.8 编号与用户原文的 6.5 不一致（已说明理由）。

---

## 第四轮：去"知识的诅咒"（术语白话化）

用户要求："不要把我认为是对你的计算机黑话非常懂，去掉'知识的诅咒'。把当前文章中的黑话或者不是人话的地方，进行修改！"

### 改动范围（全文 2042 → 2067 行）

- **新增 `### 术语速查`**（阅读指引之后）：14 行表，覆盖 Harness / Model Input / Context Assembly / execution / composition / entry / lane / operation / facet / service / replicated state / pass / effect / KV cache。阅读指引末尾加了一行指针。
- **§0 结论速览**：0–7 条全部重写为白话。"运行时编译器"改为"把 Agent 状态编译成模型输入"；"承载完整的崩溃恢复语义"→"把崩溃后恢复所需信息都记录下来"；"设计被一条不变式统治"→"被一条铁律管着"；"两段提交…崩溃点可枚举"→"先说要做→做完记录结果…崩在哪一步都能查出来"。
- **术语统一替换**：不变式 → **铁律**；总态 → **当前状态 / 叶子状态**；投影 → **整理上下文 / 挑出来重新整理**（保留一次英文 gloss）；物化 → **写进 / 写成**；逆序 dispose → **按相反顺序销毁**；闸门/准入 → 保留但加 gloss（效果准入闸门 effect gate）；单写者 → 保留并解释为"同一时刻只有一个 Drive 能推进顶层状态"；良定义 → **明确的、不会缺东西的**；契约债 → **契约上的欠账**；写放大 → 加正确 gloss（一次逻辑改动被拆成多次实际写盘）。
- **§1/§2**："编译拓扑序 / 零依赖叶子 / 横向设施" → **依赖顺序 / 不依赖任何其他包的包 / 独立工具库**；WAL 加 gloss（预写日志）；payload → **包体**。
- **§4 全章**：pass / fail-closed / 物化 / stage / materialize / flat leaf / exact-once / effect gate / mutation line / continuation / 准入目录 / replay 合成结果 等全部白话化，代码标识符与文件路径不动。
- **§5–§8**：token / stable facade / tracked proxy / path-codec / rehydrate / front-truncate / mutation log / 逃逸 / HMR / coeffect / 一等协调机制 等逐个加 gloss 或改写。
- **§9**：projection pipeline → 整理管线；durable execution first / everything is a plugin 加中文括号；cache-safe 对偶 → "缓存安全"的另一面；settlement 加 gloss；"投影链" → "整理上下文的链路"。
- **§10–§13**：13 leaf 总态 → 13 个叶子状态；orphan 加 gloss；typed schema → 有类型的 schema；规范性 → 具规范效力。

### 原则（沿用）

代码标识符、包名、API 签名、文件路径、英文原文引用**一律不动**；只改叙述文字。抽象黑话要么换成白话，要么在首次出现处加一句解释。

### 结构

无变化：仍是 13 个 `##` 主章，116 个代码围栏（配平）。

---

## 第五轮：中文黑话解释 + prompt 概念澄清 + "程序内部机制"清单 + 修正一处事实错误

用户三点要求：① 中文词汇（如"失真"）第一次出现要解释；② 围绕 prompt 说清楚；③ 详细说明哪些机制是程序内部的、不会体现在 prompt 文字上。

### ① 中文术语解释

- 阅读指引首次出现"失真"/"副作用"处，各加一段白话解释（失真 = 内容与真实发生的事对不上；副作用 = 除返回值外还改变了外部世界）。
- 术语速查表新增第二张表「中文词」：失真 / 副作用 / 落盘 / 幂等 / 原子 / 重入 / 门面 / 裸 / 退化 / 收敛 / 契约 / 状态机 / 投影。
- 术语速查表新增 prompt 相关行：prompt / system prompt / messages / Prompt Assembly / prompt template。

### ② 新增 §4.4「哪些机制只在程序内部，不会出现在 prompt 文字里」

- 判断标准：**看它最终有没有变成某条 provider message 的"内容"**。
- **表 A（15 行）程序内部、模型看不到**：operationState / usage ledger / branchTip·laneConfig·laneState / pending* / Drive·gate.admit / 单写者·写入串行线·事务 / replay 策略 / accept-drive 分离 / events·LaneSnapshot / telemetry / Chord 组装·依赖图校验·reload / service token·绑定 / replicated state·delta / protocol 分帧 / hook 的持久性分类本身。
- **表 B（9 行）真的会进 prompt**：对话历史 / 工具调用与结果 / 压缩摘要 / **system prompt（请求字段）** / **tools（请求字段）** / custom entry 经 entryProjectors / transform_context 输出 / 图片附件 / 被显式写进 entry 的运行时状态。
- 三组澄清：hook ≠ prompt 内容（只有 transition-consumed 的输出才落盘进上下文）；工具"怎么执行" ≠ 工具"结果"；replicated state ≠ 上下文。
- 因插入新节，§4.4–4.8 顺移为 §4.5–4.9，并同步修正 6 处交叉引用（§4.5 hooks→§4.6、§4.8 非目标→§4.9、§4.6 accept/drive→§4.7、§9.3 与 §9.7 与 §9.8 的 §4.5→§4.6）。

### ③ ⚠️ 修正一处事实错误（重要）

原 §9.6 写"pi 的 Model Input 同样是从 Session 整理出来的派生消息，而不是一个独立的 prompt 字段；两边高度一致"——**这是错的**。

查源码确认（`agent-loop.ts:296` 构造 `{ systemPrompt, messages, tools }`；`harness/runtime/drive/generation.ts:103` `resolveSystemPrompt`；`harness/hooks.ts:193` transform_context 可改写 systemPrompt）：

- **一次 provider 请求是三块**：`messages`（从 Session 整理）+ `systemPrompt`（请求字段，来自 lane 配置 / harness options，可以是每次请求现算的函数）+ `tools`（请求字段）。
- 因此 **pi 把 system prompt 保留为请求字段，不进入会话历史**；而 **dsh 把 prompt 提交成派生历史里的一条 `system/message`**。两边在这一点上做法**相反**，不是"独立得出同一结论"。
- 已改：§4.2 新增"请求三块"图；§4.4 表 B 两行改为"请求字段"；§9.2 主对照表「谁构造模型输入」「Prompt 是什么」两行重写；§9.3 Pi 管线图重画（左=整理，右=现算）；§9.6 对照段改为"这是两边做法不同的地方"+ 新对照表；§11 第 17 条改写并标注"本报告修正过的一处判断"；§0 第 5 条补充说明。

### ④ 新增 §5.5「system prompt 是怎么拼出来的」

`coding-agent/src/core/system-prompt.ts`（168 行）的 `buildSystemPrompt(options)`：`customPrompt` / `selectedTools`（默认 read/bash/edit/write）/ `toolSnippets` / `promptGuidelines` / `appendSystemPrompt` / `cwd` / `contextFiles` / `skills` → 一个字符串，由 `agent-session.ts` 赋给 `agent.state.systemPrompt`。因插入新节，§5.5–5.7 顺移为 §5.6–5.8。

### ⑤ 「13 个叶子状态」用词修正（用户质疑）

用户问"叶子状态是标准业界术语么"。**不是。**

- 原始出处：`harness.md:606` 英文原文 "holds one member of a **flat 13-leaf union**"；`harness.md:896` "dispatches directly from the flat `state.at` leaf"；`harness.md:1395` "each of the 13 leaves in Part 3"。
- `leaf` 是 pi 作者借树结构造的词（叶子节点 = 无子节点），指"该取值下不再挂子状态"；TypeScript 里的正式说法是"联合类型（union）的成员 / 判别式取值"。
- 全文统一改为「**13 个平铺的状态**」；§4.7 状态机处新增解释框（说明 flat / leaf 都不是标准术语、正式说法是什么、唯一例外是 ToolBatch/ToolCall 确实嵌套）；术语速查表新增一行。

### 结构

2203 行，13 个 `##` 主章，118 个代码围栏（配平）。§4 现为 4.1–4.9，§5 现为 5.1–5.8。

**最终行数修正**：加入 §5.5 正文后为 **2233 行**、120 个代码围栏（配平）。§5.5 正文依据 `coding-agent/src/core/system-prompt.ts` 全文（168 行）实读，要点：纯函数 `buildSystemPrompt(options)`；默认模板 = 固定开头 + Available tools（只列有 `toolSnippets` 的工具）+ Guidelines（按 hasBash/hasGrep/hasFind/hasLs 动态生成并去重，固定两条 Be concise / Show file paths clearly）+ pi 文档路径；再追加 `appendSystemPrompt` → `<project_context>` 块 → skills（仅 read/bash 可用时）→ `Current working directory`；`customPrompt` 整段替换默认模板；扩展可在 hook 返回 `systemPrompt` 覆盖（`agent-session.ts:1307-1313`）。

---

## 第六轮：13 点评审（P0/P1/P2）→ 四阶段重构 + Chord/Harness 边界纠正

用户给出 13 节结构化评审，总评「**这版已经值得继续打磨，而不是重写**」。执行结果：**2233 → 2586 行**。

### ⚠️ 最重要的一处事实纠正（P0-1）

原报告写"Chord 在 Harness 之外，Harness 只 `import type` chord"——**被源码推翻**。

- `packages/agent/src/harness/context.ts` **运行时** import 了 8 个值：`awaitWithContext`、`BACKGROUND_CONTEXT`、`createContextKey`、`TODO_CONTEXT`、`withAbortSignal`、`withCancel`、`withContextValue`、`withoutAbortSignal`（来自 `@earendil-works/chord/context`），外加 `import type { Context, ContextKey }`。
- `packages/agent/package.json:58`：`"@earendil-works/chord": "^0.85.1"`。
- Harness **不用** Chord 的 `FacetHost` / `Service` / `replicatedState` / remote binding。
- 正确表述（已全文统一）：**Chord 的 composition runtime 在 Harness 之上（组装它）；Chord 的 `Context` 原语在 Harness 之内（被运行时复用）。** 即"Harness 不依赖 Chord 的 composition runtime，但复用了 Chord 的 Context 原语"——复用的是"词汇表"，不是"运行时"。
- 落点：§2.2 新增"两种关系"图 + 8 值 import 清单；§7.2 加注；§7.10 表格行与论证段重写。

### P0-2：Prompt Assembly → 四阶段 Model Request Construction

新术语（已写入 阅读指引 + 术语速查 + §0 第 0 条）：

```
① Context Projection    readBoundedContext → buildSessionContext
② Request Assembly      prepareGeneration：resolveSystemPrompt + activeToolNames → tools + before_request
③ Provider Encoding     streamHarnessAssistant：transform_context → toProviderMessages → before_payload
④ Effect Execution      gate.admit(() => models.streamSimple(...)) → settlement
```

- 阅读指引新增 4 阶段 ASCII 图 + "阶段 → pi 源码位置 → 详见章节"映射表。
- §4.1 整条链重写为带阶段分隔的流程图；小节索引表加入 4.5。
- §4.3 重写为"阶段 ①"，4 行动作表（scanBranch → reverse → buildContextEntries → buildSessionContext）+ 逐 entry 类型产出表。
- §4.10 标题与结论句改为「可恢复的 **Model Request Construction** Runtime」；小结表补 ①②③ 三行。
- **"5 步固定算法"全部替换**（原 §4.3 是 5 步，重构后是 4 步）：§4.2 图注、§9.2 表、§9.3 图、§9.3 差异表、§9.4 铁律列表。
- **术语分工（关键）**：`Context Assembly` 只保留在**控制权层面**（"谁拥有 Context Assembly 的最终解释权"），指具体机制时一律用"模型请求构造"。已写进术语速查表并加读法提醒。

### P0-3：插件入口从"只有两个"改为三类（§8.4）

原句"插件真正能写进模型视野的入口只有两个（写 entry、注册 hook）"是绝对化错误。改为三类表：

| 类别 | 入口 | 落点 |
|---|---|---|
| A. 写持久历史 | 追加 Session entry（message / branch_summary / custom） | ① Context Projection |
| B. 参与构造过程 | `before_request`（可改 streamOptions）、`transform_context`（可改 messages/systemPrompt）、`before_payload` | ②③ |
| C. 提供显式资源 | `systemPrompt`、`tools`、`skills`、`promptTemplates`、`projectors` | ① 或 ② |

结论改为"关键不是入口有几个，而是这三个入口都在受控链上"。

### P0-4：新增 §4.5「Resource 也是模型输入的一部分：以 Skill 为例」

- `AgentHarnessResources<TSkill, TPromptTemplate>`（`harness/types.ts:73`）= `{ promptTemplates?, skills? }`，注释原话 "Resources made available to explicit invocation methods and system-prompt callbacks"。
- `Skill`（`harness/types.ts:49`）= `{ name, description, content, filePath, disableModelInvocation? }`——**`content` 默认不进模型**。
- **同一份数据两种命运**：① 目录级 → `formatSkillsForSystemPrompt()` 产出 `<available_skills>` 块进 **system prompt**（只有 name/description/location）；② 显式激活 → `formatSkillInvocation()`（`harness/skills.ts:39`）产出 `<skill name location>content</skill>`，在 `runtime/lane.ts:537` 变成一条 **user 消息**进会话历史。
- `PromptTemplate` 走同一条路（`formatPromptTemplateInvocation`，`prompt-templates.ts:268`）。
- §5.7 的 Skills 条目已加交叉引用指向 §4.5。

### P1：§9 提升到 Assembly Ownership

- §9.2 主表新增行「**谁拥有最终解释权（assembly ownership）**」；表后新增核心判断段：「**谁拥有 Context Assembly 的最终解释权**」——Pi 收进内核（封闭介入集合），dsh 交给 plugin graph。
- 新增独立小节 `#### Prompt 是 request field，还是 derived history？`：Pi 三块（messages=derived history，systemPrompt/tools=request field，源码 `agent-loop.ts:296`）；dsh prompt 提交为 `system/message` 表面节点 = 派生历史的一部分。点明这是**本体论选择**，不是优劣。
- §9.3 标题 `两张 Prompt Assembly 图` → `两张构造管线图`。

### P1/P2 其余

- **基线**：头部加注——本工作区快照 `71dca871b`（2026-09-11）为准；评审指出上游 main 已到 `3349e1db`（2026-09-15），**本机无网络未能独立核实**，明确标注不可当作已验证事实。
- **§7.2 能力边界限定**：加警告框——"capability boundary" 指的是**组装期 / 进程边界**上的能力授予，**不等于完整安全授权系统**（不做身份、不做策略引擎、不做审计、不防两个插件合谋）。是"下限"而非安全模型全部。
- **新增 §12「收束：从 Execution / Composition 到四层模型」**（原 §12/§13 顺移为 §13/§14）：
  - L0 Durable Truth → L1 Context Projection → L2 Model Request Construction（含 ②③）→ L3 Effect + Settlement
  - 论证"execution / composition 是两个**横切关切**，不是架构分层"；四层模型能定位"分歧具体发生在哪一层"。
  - 给出自上而下的排障读法（L3→L0 倒查）。
- §11 第 15/16 条措辞对齐新词汇。

### 环境坑

- **同一文件的多个 Edit 并行调用会静默丢改动**（只有部分生效，但每个都报 success）。批量改同一文件必须**串行**逐个 Edit。本次因此在 §4.2/§6.8/§7.4/§9 上重做了两遍。
- `grep` 在 zsh 下带 `--include` 会 "no matches found"；用 Grep 工具。

### 结构

2586 行，14 个 `##` 主章。§4 = 4.1–4.10，§9 含新增 `####` 小节，§12 为新增收束章。
