# 2026-09-16

## pi2 研究报告第二遍编辑（教学层补齐）

用户对 1434 行版给出 10 点评审，明确「不需要大改结构，只做第二遍编辑：每个 Chord 概念先用现实问题讲清楚，再放 API / 源码细节」。执行结果：**1434 → 1538 行**。

### 已改（用户点名 3 项 + 强信号 4 项）

1. **§7.1 前置具体 composition 场景**（用户点名 #1）：开头加 worker 进程 + 三个需求（TUI 显示进度 / RPC 远程发起 run / 插件注册 service）→ 直接暴露 Session/Harness/tool registry 的三个坏结果 → 逼出问题陈述"一个已可靠运行的 Agent 如何被拆开、连接、扩展，同时不暴露内部能力"。
2. **§7.3 补 `Plugin → Facet → Service` 三层关系**（用户点名 #3）：加三层对照块 + 金句「**Facet 是"东西住在哪里"，Service 是"它对外能做什么"**」。
3. **§7.4 补"为什么不直接传对象"对比**（用户点名 #2）：TUI→models 对象 vs TUI→Models Service→本地/远端实现；并前置 2 句 DI 定位（用户 #6）："Chord 是 DI 的远亲，关心能力在哪/谁提供/何时存在/谁能拿到"。
4. **§7.2 把"能力边界"提为 Chord 的第二核心定义**（用户 #4，称这是全文最有特色的观察）：新增「Chord 有两个核心问题」块（①怎么装起来=composition ②装起来后能拿到什么=capability boundary），并明确"第 2 问才是本报告认为最有价值的观察"；§0 结论速览第 6 条、§8.4 开头同步对齐。
5. **软化 Cordis/Chord 切割**（用户 #7）：§7.11 与 §9.4 的"程序内 vs 跨进程"改为"重心差异"，并加显式警告「**重心不同 ≠ 层级不同**」「读成两个互不相干的抽象层是本节最想避免的误读」；§9.4 主图标签改为"进程内的组装与插件树 / 跨环境的能力边界与状态"。
6. **主图贯穿全文**（用户 #9）：§7.6/§7.7/§7.9/§7.10 各加一句"回到 §2.2 主图"的回指；§7.9 明确定位为**本章总结图**。
7. **§3 降级为主线外**（用户 #10）：标题改为「`pi-ai`：Agent Runtime 依赖的模型层（主线外，可跳过）」，内容压成"AgentHarness 真正依赖的四件事"表（Models/ModelRuntime、AssistantMessageFrame、deferred handle、只收 function calling），删掉 provider 工厂/codex/overflow 等细节。

### 两层阅读约定（用户 #5，轻量版）

§7 章首加「阅读约定」：每个概念先用现实问题讲清、再用 `#### 实现细节` 展开；第一次读可跳过所有实现细节。已在 §7.3 / §7.4 / §7.5 插入 3 个 `#### 实现细节` 小标题（§7.5 的为「实现细节：Delta tracking」）。

### 未做

- 用户 #5 的"每章严格分两层"只做了 §7.3/7.4/7.5 三处（避免过度改结构）；§7.6–7.8 未加小标题。
- §3 未真正降级为子节（那需要 §4–§13 全部重编号，与"不需要大改结构"冲突），改为标题标注 + 内容压缩。
- §7.11 与 §9 的 DSH 对照内容未再改（用户 #7 只要求软化措辞）。
